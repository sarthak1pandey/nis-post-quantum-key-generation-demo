# 2-Minute Professor Demo Script

"Professor, let me show you real post-quantum file encryption:

1. Set `Adaptive` mode and push `HNDL Risk` high. The ML selector chooses `HYBRID`.
2. Start and watch all 11 crypto phases complete in real time.
3. In **Real File Encryption Demo**, drag `demo-files/resume.pdf`.
4. Click **Encrypt with Session Key** and download `resume.pdf.pqc`.
5. Show file attack simulation:
   - If quantum breaks ECDHE, hybrid still survives via ML-KEM.
   - If PQC weakens, hybrid still survives via ECDHE backup.
   - If one file key leaks, ratcheting protects future files.
6. Click **Decrypt & Verify** and download the restored original file.

This is real AES-256-GCM file encryption and decryption using HKDF-derived session keys from the PQC/classical handshake."
